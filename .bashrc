
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

alias ..='cd ..'
alias cd..='cd ..'
alias la='ls -la'
alias ls='ls --color=auto'
alias ll='ls -l'
alias gti='git'

